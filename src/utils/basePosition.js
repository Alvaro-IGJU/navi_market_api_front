// Configuración de la posición de la base
export const getBasePosition = () => {
  return {
    position: [0, -20, 30], // Coordenadas de la base
    scale: [10, 10, 10], // Escala de la base
  };
};